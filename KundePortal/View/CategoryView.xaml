<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:viewmodel="clr-namespace:KundePortal.ViewModel"
    NavigationPage.HasBackButton="False"
    x:Class="KundePortal.View.CategoryView">

    <!--    Viewmodel instance-->
    <ContentPage.BindingContext>
        <viewmodel:CategoryViewModel></viewmodel:CategoryViewModel>
    </ContentPage.BindingContext>

    <!--    Toolbaritem create new account-->
    <ContentPage.ToolbarItems>
            <ToolbarItem 
                Text="Konto"
                Command = "{Binding AccountCommand}"/>
    </ContentPage.ToolbarItems>
    

    <ContentPage.Content>
        <ListView SelectedItem="{Binding MainCategory}" ItemsSource="{Binding AllCategories}" HasUnevenRows="true">
        </ListView>
    </ContentPage.Content>
    
</ContentPage>

<!--    <ContentPage.ToolbarItems>
        <ToolbarItem Name="KontoItem" Text="Konto" Activated="KontoItem_Activated"></ToolbarItem>
    </ContentPage.ToolbarItems>

    <StackLayout>
        <SearchBar Placeholder="Søg..."></SearchBar>

        <ListView x:Name="listViewCategories" ItemSelected="listItemClicked">

        </ListView>
    </StackLayout>-->